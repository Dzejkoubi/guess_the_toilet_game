import 'package:flutter/widgets.dart';

class PauseMenu extends StatelessWidget {
  static const String id = 'PauseMenu';

  const PauseMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Pause button');
  }
}
